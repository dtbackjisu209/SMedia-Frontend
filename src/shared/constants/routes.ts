export const ROUTE_PATHS = {
  login: '/login',
  register: '/register',
  feed: '/',
  userProfile: '/users/:userId',
  postDetail: '/posts/:postId',
  notifications: '/notifications',
  chat: '/chat',
} as const
